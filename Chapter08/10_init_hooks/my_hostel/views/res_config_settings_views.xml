<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.hostel</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="5"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Hostel" string="Hostel" name="my_hostel" groups="my_hostel.group_hostel_manager">
                    <block title="Hostel Management" name="hostel_setting_container">
                        <setting string="Hostel" company_dependent="1" help="Allow users to hostel user">
                            <field name="group_hostel_user"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="hostel_config_settings_action" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'module' : 'my_hostel', 'bin_size': False}</field>
    </record>

    <menuitem name="Settings" id="hostel_setting_menu" parent="hostel_main_menu" action="hostel_config_settings_action" sequence="50"/>

</odoo>
